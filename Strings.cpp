#include<iostream>
using namespace std;

int main(){
    string s;
    cout<<"Enter a string: ";
//    cin>>s;
    getline(cin,s);
    cout<<s;
//it ll not consider the second word which is given as input to it.
//INSTEAD OF cin>> HV TO USE getline(cin,name of var in which want to store!)


}
